import { Constructor } from 'type-fest';
/** Wraps the JavaScript built-in "instanceof" in a type guard assertion. */
export declare function assertInstanceOf<InstanceType>(
/** The value to check. */
instance: unknown, 
/** The constructor that the "instance" input will be checked against. */
classConstructor: Constructor<InstanceType>, 
/** Message to include in error message if this assertion fails. */
failureMessage?: string | undefined): asserts instance is InstanceType;
/** Wraps the JavaScript built-in "instanceof" in a type guard. */
export declare function isInstanceOf<InstanceType>(
/** The value to check. */
instance: unknown, 
/** The constructor that the "instance" input will be checked against. */
classConstructor: Constructor<InstanceType>): instance is InstanceType;
/** Asserts that the given input is defined (not null and not undefined) */
export declare function assertDefined<T>(
/** The value to check. */
input: T, 
/** Message to include in error message if this assertion fails. */
failureMessage?: string | undefined): asserts input is NonNullable<T>;
/** Checks that the given input is defined (not null and not undefined) */
export declare function isDefined<T>(
/** The value to check. */
input: T): input is NonNullable<T>;
